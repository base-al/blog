package core

// init core modules and setup routes
func InitModules() {
	// Initialize modules
	//registry := registry.NewRegistry()
	//
	// // Register modules
	//registry.RegisterModule("admin", admin.NewModule(db, cfg))
	// registry.RegisterModule("comments", comments.NewModule(db, cfg))
	//

}
